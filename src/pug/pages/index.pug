extends ../layout/main
    
block variables
    - title= "Webpack template"

block content
    .content-wrapper
        img(src="assets/img/logo.png")
        h1 webpack-template 
        p now with pug 🐶
        small (and w/o vue)
