extends ../layout/main
    
block variables
    - title= "Webpack template"

block content
    .content-wrapper
        img(src="assets/img/logo.png")
        include ../blocks/header/header.pug
        p now with pug 🐶
        small (and w/o vue)
